{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/tooppoo/git-kura/main/internal/cli/schema/get_data.schema.json",
  "title": "git kura get — envelope data",
  "description": "Command-specific schema for the data payload of `git kura get --json` / `--format json`, nested under the common envelope's data field.",
  "type": "object",
  "required": [
    "schemaVersion",
    "key",
    "kind",
    "branch",
    "worktreePath",
    "repositoryRoot",
    "baseBranch",
    "exists",
    "dirty"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "Schema version of this output."
    },
    "key": {
      "type": "string",
      "description": "The key passed to the command.",
      "allOf": [
        { "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$" },
        { "not": { "pattern": "\\.\\." } },
        { "not": { "pattern": "\\.$" } },
        { "not": { "pattern": "\\.lock$" } }
      ]
    },
    "kind": {
      "type": "string",
      "enum": ["worktree"],
      "description": "Workspace kind."
    },
    "branch": {
      "type": "string",
      "minLength": 1,
      "description": "Git branch name associated with the key."
    },
    "worktreePath": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute path to the worktree."
    },
    "repositoryRoot": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute path to the repository root."
    },
    "baseBranch": {
      "type": "string",
      "minLength": 1,
      "description": "Branch the worktree was created from."
    },
    "exists": {
      "type": "boolean",
      "description": "Whether the worktree currently exists on disk."
    },
    "dirty": {
      "type": "boolean",
      "description": "Whether the worktree has uncommitted changes."
    }
  }
}
